# Copy to .env.local for local dev. Points at the FastAPI backend --
# see backend/README section on running the API locally, or the deployed
# Render URL once live.
NEXT_PUBLIC_API_BASE=http://localhost:8000
